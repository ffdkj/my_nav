# 复制为 .env 后按需修改（.env 已被 .gitignore 忽略）

# 容器把 8080 发布到哪个地址/端口
# - 100.70.0.29 = 本机 Tailscale 虚拟 IP（其他 tailnet 设备可访问）
# - 127.0.0.1    = 仅本机（配合 tailscale serve 出 HTTPS 时用这个）
NAV_BIND_IP=100.70.0.29
NAV_PORT=8090

# 日志级别：debug | info | warn | error
NAV_LOG_LEVEL=info
